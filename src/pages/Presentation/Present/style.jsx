import styled from "styled-components";

const Styled = styled.div`
  .presentPresentation-container {
    background-color: #fff;
    min-height: 800px;
    display: flex;
    align-items: center;
    justify-content: center;
    flex-direction: row;
    gap: 50px;
  }
  .left-icon,
  .right-icon {
    cursor: pointer;
  }
`;

export default Styled;
